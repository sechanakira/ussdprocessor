pipeline {
  agent any
  
  stages {
    stage("source checkout") {
      steps {
        echo 'CHECKING OUT CODE!!!'
      }
    }
    stage("build") {
      steps {
        echo 'BUILDING!!!'
      }
    }
  }
}
